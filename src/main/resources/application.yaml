spring:
  application:
    name: webflux-guide
  webflux:
    base-path: /api
  r2dbc:
    url: r2dbc:mysql://localhost:3306/reactive_login
    username: reactive_login
    password: Frustrating@254#
    port: 3306
    host: localhost
    database: reactive_login
    driver: mysql

  logging:
    level:
      org:
        example: DEBUG
        springFramework:
          web: INFO
          data:
            r2dbc: DEBUG
      reactor:
        netty:
          http:
            client: DEBUG